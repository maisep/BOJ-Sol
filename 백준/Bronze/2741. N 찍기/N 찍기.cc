#include <iostream>

using namespace std;

int main() {
    cin.tie(NULL);
    ios::sync_with_stdio(false);

    int A;
    cin >> A;

    for (int i = 1; i <= A; i++) {
        cout << i << '\n';
    }

    return 0;
}